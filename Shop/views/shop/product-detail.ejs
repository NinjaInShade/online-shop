<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <div class="max-width m-auto px-2 mt-3">
        <div class="mt-5">
            <h1><%= product.title %> | £<%= product.price %></h1>
            <hr>
            <img src=<%= product.imageUrl %> class="product-detail-img" alt="Product image for <%= product.title %>">
            <h3 class="mt-3"><%= product.description %></h3>
            <form action="/cart/<%= product.id %>" method="POST" class="flex-grow-1 ms-1 mt-3">
                <button class="btn btn-primary w-25" type="submit">Add to cart</button>
            </form>
        </div>
    </div>

<%- include('../includes/end.ejs') %>